/*let Promise = require('bluebird');

let DiscordInterface = require(`./src/discordInterface.js`);

let ESI = require(`./src/esi.js`);

var options = {
  route: 'search',
  parameters: 'categories=solarsystem&search=jita'
}


ESI.GetItemPrice('D-PNP', 'caraacal')
  .then(console.log)
  .catch(err => console.log(err.message))
//  .then(console.log);

ESI.Get(options)
  .then(console.log)
*/
/*
DiscordInterface.EvaluateCommand('time','1','2')
  .then(x => console.log(`result is: ${x}`))
  .catch(x => console.log(`error is: ${x}`))
*/
